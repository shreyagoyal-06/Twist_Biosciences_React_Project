'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _MediaQueryList = require('./MediaQueryList');

var _MediaQueryList2 = _interopRequireDefault(_MediaQueryList);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const listenerTrigger = evalQuery => (currentlisteners, mqList) => {
  const matches = evalQuery(mqList.media);

  if (mqList.matches === matches) return;

  Object.defineProperties(mqList, {
    matches: { enumerable: true, configurable: true, value: matches }
  });

  currentlisteners.forEach(listener => listener(mqList));
};

exports.default = listenerTrigger;