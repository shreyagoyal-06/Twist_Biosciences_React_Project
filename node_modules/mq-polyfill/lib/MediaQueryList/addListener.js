'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
const createAddListener = listeners => function addListener(listener) {
  if (!listeners.has(this)) listeners.set(this, []);

  const currentListeners = listeners.get(this);

  if (currentListeners && !currentListeners.includes(listener)) currentListeners.push(listener);
};

exports.default = createAddListener;