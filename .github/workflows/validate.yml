name: Component Tests

on:
  push:
    branches:
      - testCase_feature  # run only on your feature branch
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: Install Dependencies
      run: |
        npm install
        npm install @testing-library/react @testing-library/jest-dom --save-dev
        
    - name: Run Tests
      run: npm test
      env:
        CI: true
        NODE_ENV: test
